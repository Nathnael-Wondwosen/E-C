# User Service Environment Variables Example
#
# This service is currently optional and only exposes `/` and `/health`.
# It is not part of the main production request path.

PORT=3007
MONGODB_URI=mongodb+srv://username:password@cluster.example.com/database?appName=App
JWT_SECRET=replace_with_a_long_random_secret
